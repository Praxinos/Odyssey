Minimal Requirements:

- Install Python 3.9+
- pip install sphinx
- pip install sphinx-material
- pip install myst-parser
- pip install sphinx-copybutton
- pip install sphinx-markdown-tables

Build the documentation:

On Windows:
    in cmd : make.bat html
    in bash : ./make.bat html

On Mac:
    make html